Las instrucciones para crear un ambiente virtual para implementar FastAPI está en la documentación oficial:
https://fastapi.tiangolo.com/virtual-environments/#install-packages-directly


REALIZAR LOS PASOS DEL 1 AL 7 PARA CADA DISPOSITIVO DIFERENTE EN EL QUE CLONÓ EL REPOSITORIO

 - Asegurarse de esto antes de hacer un push!
Iniciar repositorio en git y agregar al .gitignore el archivo .venv
echo "*" > .venv/.gitignore
 - - - - 

EJECUTE EL PASO 1,3 SOLO SI ES LA PRIMERA VEZ - HACER EL RESTO DE PASOS SIEMPRE
1. crear entorno virtual: asegurese de estar ubicado en el repositorio del proyecto
ejecute: python -m venv .venv

2. Activar el entorno virtual: hacer esto cada que inicias la terminal para ejecutar tu proyecto
terminal bash: source .venv/Scripts/activate
terminal powerShell: .venv\Scripts\Activate.ps1

3. Actualizar a la última version el pip
python -m pip install --upgrade pip



4. Instalar los paquetes por medio de requirements.txt:
pip install -r requirements.txt


-----


Ejecucion del repositorio:
uvicorn main:start

-----

SONARQUBE LOCAL 

Se necesita globalmente instalar pysonar (?)

1. doble click a StartSonar.bat dentro de la carpeta de SonarQube: C:\Users\rtsus\Documents\Sonar\sonarqube-26.4.0.121862\bin\windows-x86-64


2. Log dentro de http://localhost:9000  con credenciales de sonarqube, hacer esto ejecutando el archivo .bat anteriormente mencionado

3. Para ejecutar un Scann:  (ojo, debes tener instalado pysonar)
pysonar \
  --sonar-host-url=http://localhost:9000 \
  --sonar-token=My_Token \
  --sonar-project-key=test

remplazar My_Token con el token que generaste en sonarqube al activar credenciales
